# Create environment and install with:
#   mamba create -n poregen python=3.11
#   mamba activate poregen
#   mamba install pytorch
#   pip install -r requirements.txt
#   pip install -e ".[dev]"
#
# Installed torch/torchvision pair on the DGX Spark box:
#   torch        2.12.1+cu130
#   torchvision  0.27.1+cu130
#
# torchvision is deliberately NOT a requirement line below. Only FID needs it,
# and a plain resolve pins a different torch and silently replaces the working
# build. Install it separately, version-matched, with --no-deps:
#   pip install --no-deps torchvision==0.27.1+cu130 \
#       --index-url https://download.pytorch.org/whl/cu130
# See docs/DEVELOPMENT.md, "torchvision (FID only)", for the pairing table.

# Core data / I/O
numpy>=1.24
zarr>=2.16
numcodecs>=0.12
tifffile>=2023.7
pyarrow>=14.0
pandas>=2.0

# Scientific
scipy>=1.11
scikit-image>=0.21
scikit-learn>=1.4
umap-learn>=0.5

# Utilities
tqdm>=4.65
psutil>=5.9
fill-voids>=2.0
joblib>=1.3
pyyaml>=6.0

# Vendored (see third_party/RR_layer, MIT licensed, pinned at commit
# de2e8a5035cf0d5897a59f7f743fe8c3655a7caa) — RRLayer used by the VRRAE VAE
# bottleneck (v2.vrrae). Local deviations: third_party/RR_layer/PATCHES.md.
# Install editable: pip install -e third_party/RR_layer
-e ./third_party/RR_layer

# Dev
pytest>=7.0

# Notebook / monitoring
jupyterlab>=4.0
ipykernel>=6.0
matplotlib>=3.7
ipywidgets>=8.0
tensorboard>=2.14
