extends: ldm01/base
experiment:
  name: ldm01
  variant: abl_nb_avail
  description: >-
    Ablation C — neighbor latents + per-direction availability state embeddings,
    no position or porosity scalar conditioning. BFS generation order.

model:
  use_pos_cond: false
  use_por_cond: false
