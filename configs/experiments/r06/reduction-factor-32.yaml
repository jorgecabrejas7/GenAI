experiment:
  name: r06
  variant: reduction-factor-32
  description: >-
    R06 latent-compression sweep point: total voxel reduction 32x
    (z_channels=2 at f=4 spatial downsampling). XCT-only single-branch setup shared via r06/base.

extends: r06/base

overrides:
  model:
    z_channels: 2
