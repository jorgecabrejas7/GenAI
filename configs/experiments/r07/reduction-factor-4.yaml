experiment:
  name: r07
  variant: reduction-factor-4
  description: >-
    R07 full-AE (XCT+mask in/out) sweep point: total voxel reduction 4x
    (z_channels=16 at f=4 spatial downsampling). See r07/base.

extends: r07/base

overrides:
  model:
    z_channels: 16
